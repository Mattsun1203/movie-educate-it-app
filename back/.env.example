# prisma migrate dev / prisma studio などCLI操作専用（アプリ本体はHyperdrive経由で接続する）
# ローカル開発はdocker-compose.ymlのPostgreSQLを対象にする
DATABASE_URL="postgresql://codestep:codestep@localhost:5434/codestep?schema=public"

# 未設定時はNODE_ENVに応じたデフォルト（development: debug / production: info / test: silent）を使う
LOG_LEVEL=
